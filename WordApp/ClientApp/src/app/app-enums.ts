export module Enums {
  export enum EUserType {
    Pupil = 1,
    Administrator = 2,
  }

  export enum ETaskStatus {
    Unknown = 0,
    Open = 1,
    InProgress = 2,
    Done = 3,
    Failed = 4,
  }
}
